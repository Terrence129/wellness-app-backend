# Local PR local-002: Schema And Flyway

- Source branch: `feature/schema-flyway`
- Target branch: `master`
- Merge commit: `32cda71`
- Status: Merged

## Summary

Adds Flyway migrations for `users`, `wellness_logs`, and `ai_advice`, seed data, and JPA entities/repositories.

## Verification

- `mvn -DskipTests compile`
